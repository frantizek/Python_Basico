# Arrays are collections of elements, stored at 
# contiguous memory locations, indexed by integers.

# Creating an array
arr = [1, 2, 3, 4, 5]

# Accessing elements
print(arr[0]) # Output: 1

# Updating elements
arr[0] = 100

# Length of array
print(len(arr))
